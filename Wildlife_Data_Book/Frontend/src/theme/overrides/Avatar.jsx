export default function Avatar(theme) {
  return {
    MuiAvatar: {
      styleOverrides: {
        root: {
          // backgroundColor: theme.palette.secondary.main,
        },
      },
    },
  }
}
